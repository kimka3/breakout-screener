# 생성물 — 매 실행마다 다시 만들어진다
report.html
report.artifact.html
breakouts*.csv

# 캐시 (시세 pickle, 구성종목 CSV, 사내 CA 번들) — 29MB, 재생성 가능
.cache/

__pycache__/
*.pyc

# 이 PC 전용 권한 설정 — 절대경로가 들어있어 공유하지 않는다
.claude/settings.local.json
